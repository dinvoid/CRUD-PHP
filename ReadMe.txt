It is created using PHP and BOOTSTRAP and Vanilla Javascript.

-------------------------------------------------------

NOTE!!!

1. Create a database naming "user";
2. Import the sql "adduser.sql" into the "user" database you create. 

THE "adduser.sql" is inside to the folder "database" along with the php files and css.


-------------------------------------------------------

his project aims to create CRUD Application websites using php, bootstrap, vanilla js,mysql. Using this project you can easily create "Data Views" which would serve as CRUD interfaces where you can manipulate data from a SQL Server database such as create, insert, update, delete, 
